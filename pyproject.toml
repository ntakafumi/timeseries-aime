[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tsaime"
version = "0.3.3"
description = "Forecast-aligned scalar and vector-output Approximate Inverse Model Explanations for time series"
readme = "README.md"
requires-python = ">=3.9"
authors = [
  {name = "Takafumi Nakanishi"},
  {name = "Suwanna Rasmequan"},
  {name = "Watcharaphong Yookwan"},
  {name = "Krisana Chinnasarn"}
]
license = "PolyForm-Noncommercial-1.0.0"
license-files = ["LICENSE.txt"]
keywords = [
  "explainable artificial intelligence",
  "empirical dynamic modeling",
  "time series",
  "inverse operator"
]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Science/Research",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Scientific/Engineering :: Artificial Intelligence"
]
dependencies = [
  "numpy>=1.23",
  "pandas>=1.5"
]

[project.optional-dependencies]
smap = [
  "pyEDM==2.5.7"
]
plot = [
  "matplotlib>=3.7"
]
notebook = [
  "ipykernel>=6",
  "jupyterlab>=4",
  "nbformat>=5"
]
test = [
  "pytest>=7",
  "build>=1"
]
all = [
  "scikit-learn>=1.2",
  "matplotlib>=3.7",
  "pyEDM==2.5.7",
  "ipykernel>=6",
  "jupyterlab>=4",
  "nbformat>=5",
  "pytest>=7",
  "build>=1"
]

[project.urls]
Homepage = "https://github.com/ntakafumi/timeseries-aime"
Repository = "https://github.com/ntakafumi/timeseries-aime"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-ra"
