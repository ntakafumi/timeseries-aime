# v9.4の説明検証方針

2026年9月6日。
v9.3を見た後の探索的な追加検証であり、未観察データによる事前登録実験ではない。
採択や有意差を実行成功の条件にしない。

## 固定する対象

Time Series AIME（ts-AIME）の線形逆作用素を維持する。
学習期間、検証期間、最終評価期間、入力10変数、1/6/24時間の予測、S-Mapの選択手順を変更しない。
共通パッケージはtsaime 0.3.3を使用する。
0.3.2からの変更は日時の内部精度に依存しない暦グリッド判定だけであり、逆作用素の式や推定方法は変更しない。
APR固有の追加処理はv9.4ノートブックに置く。
v9.3のノートブックと結果を上書きしない。

## 数学的な説明対象

過去窓wだけから平均と尺度を求め、標準化入力Xと予測出力Yに対して

\[
A_w=S_{XY,w}(S_{YY,w}+\lambda I)^\dagger
\]

を推定する。
次の7日間の入力側パターンを、元の単位で

\[
\widehat x=\mu_{X,w}+D_{X,w}A_wD_{Y,w}^{-1}(y-\mu_{Y,w})
\]

と再構成する。
変数jと予測時間hについての積A[j,h]zY[h]は、標準化した入力再構成への加法的な項である。
PM2.5予測への特徴寄与でも、介入の効果でもない。
三つの項の和が標準化再構成と一致することを検査する。

## 単純な対照

現在PM2.5のみ、および現在/1時間前/24時間前のPM2.5からの線形逆回帰を比較する。
正則化は検証期間内だけで、気象7変数の標準化誤差により選ぶ。
PM2.5履歴を自分自身から再構成する自明な結果は比較対象にしない。
ts-AIMEと対照は同一の較正窓と評価時刻を使う。

固定逆作用素は検証期間で推定する。
その元単位の傾きB0=DX0 A0 DY0^{-1}を固定したまま、過去窓の入力平均と予測平均だけを更新する対照を追加する。
もう一つの対照はA0を固定して、過去窓の平均と尺度を更新する。
前者は切片更新、後者は標準化された係数を固定したアフィン適応を表す。
それらと全更新を同一評価時刻で比較し、平均、尺度、作用素の更新を区別する。
結果が不利な場合も残す。

## 説明の安定性

各28日較正窓を6時間等間隔の暦に戻し、欠測行を消して時間を詰めない。
1日と7日の非循環移動ブロック再標本化で、正則化を固定して逆作用素を再推定する。
元単位の傾きを元の較正窓の標準化座標に戻して比較する。
再標本化ごとの異なる尺度で係数を比較しない。
入力変数間の係数行ノルム順位、係数の符号一致率、次期間の再構成の変動を別に報告する。
予測器の再学習不確実性は含まない。
28日の窓に含まれる7日ブロックは少ないため、精密な信頼区間や真の符号の保証とは呼ばない。
一定の入力、一定の出力、再標本化中に変動が失われた場合を記録し、見かけ上安定なゼロと区別する。

## 気象状態の説明と事例

すべてのサイトで、評価開始以前の情報だけから、次期間の再構成、予測時間別の項、S-Mapの局所順方向係数を保存する。
公開用表は集計とし、時刻ごとの観測値を含むトレースはprivate_tracesに置く。

検証期間の現在PM2.5の75パーセンタイルを高濃度の操作的閾値とする。
予測差y24-y1の検証期間の25/75パーセンタイルを、変化が相対的に小さい群と大きい群の閾値とする。
相対的に小さい群でも予測差が正の場合があるため、低下群とは呼ばない。
これは環境基準でも、実測の増減を保証する分類でもない。
最終評価のすべての該当時刻と、連続する該当時刻をまとめた事例を集計する。
予測パターンの比較は現在PM2.5の検証期間の四分位層別でも出力する。
事例と時刻は独立標本ではない。
説明図の区間は、各条件で最初に現れる適格な事例を使い、誤差や有意差で選ばない。
条件に該当する事例がなければ「該当なし」と記録する。

## 図表と解釈

降雨などが一定の窓では、入力変動に関する係数を解釈対象から外す。
季節集計には変動のある窓数と一定の窓数を併記する。
地域間の図には共通の色尺度を使い、北京を12の独立地域と扱わない。
S-Map係数、逆再構成係数、変数除去による予測改善を同一の重要度と呼ばない。
二次回帰は補足的な非線形再構成診断に限定し、独立した既存XAIやKernel-AIMEの実装とは呼ばない。

## 実行と監査

既定のautoモードでは、設定、ソース記録、保存ファイルのハッシュが一致するv9.3実行だけを再利用する。
不整合な実行を黙って再利用しない。
fullモードではデータ監査から全実験を行う。
どちらも新しい時刻付きv9_4_aprディレクトリに、完全なCSV、TeX、図、検証結果、ZIPを作る。
演算の整合性と科学的結論を別の表にし、結論を機械的な合否条件にしない。
NIESの利用条件の表記不一致は、コード変更では解消しない公開前の確認事項として残す。

## 先行研究との位置付け

出力から入力パターンを求める考え方自体を世界初としない。
単一出力の相関との一致、多変量逆回帰との関係を明記する。
新規性の候補は、複数時間のPM2.5予測に対応する気象状態を時間局所的に説明し、その有効範囲を時間順序を守って検証することである。

関連する一次資料：

- Haufe et al. (2014), https://doi.org/10.1016/j.neuroimage.2013.10.067
- Kindermans et al., PatternNet and PatternAttribution, https://arxiv.org/abs/1705.05598
- Deyle et al. (2016), https://doi.org/10.1098/rspb.2015.2258
- Nakanishi (2026), AIME², https://doi.org/10.1038/s44488-026-00004-0
- Hou et al. (2022), https://doi.org/10.1021/acs.estlett.1c00865
