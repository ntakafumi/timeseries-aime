# v9.4検証記録

検証日：2026年9月6日。
対象：`notebooks/tsAIME_APR_all_experiments_v9_4.ipynb` と `tsaime 0.3.3`。
実行手順は [V9_4_RUNNING_ja.md](V9_4_RUNNING_ja.md) に記載した。
このコード配布には実験結果と執筆用の結果解釈メモを含めない。

## 実行確認の範囲

| 確認 | 実施内容 |
|---|---|
| 論文用の追加検証 | publication、北京12地点とつくば1地点、664較正窓 |
| 基礎実験の扱い | 監査済みv9.3の設定とソース記録を照合し、306成果物と29非公開トレースのハッシュを検証して再利用 |
| 最初からの経路 | full / smoke、北京2地点とつくば、データ監査、合成実験、予測、追加説明検証、図表、ZIPまで実行 |
| 縮小実行の較正窓 | 193窓。論文用数値に混ぜない |
| テスト | Python 3.13.2、pandas 2.3.3と3.0.2の2環境で各55件成功 |
| 配布用コピー | 公開用ZIPを別フォルダへ展開し、wheel単体からの読み込みと55件のテストを確認 |
| Jupyter実行 | 配布用コピーを新しいカーネルで実行し、全15コードセルが順番に完了。full / smokeでPASS |
| 図 | 13点のPDFをレンダーし、表示を目視確認 |
| 表 | 46点のTeXをまとめてコンパイルし、主要比較表と安定性表を目視確認 |
| コードと出力 | ノートブックのコードハッシュと実行記録を照合。出力マニフェストとZIPの整合性を確認 |

論文用の追加解析の最終実行IDは `20260906T084857582009Z` である。
最初から計算する縮小確認の実行IDは `20260906T084858463641Z` である。
論文用の全13地点の予測器を今回すべて最初から再学習した、という確認ではない。
全規模の基礎実験はv9.3の実行済み記録に基づき、v9.4の追加解析を新規実行した。
fullモードは、再利用元のv9.3がなくても同じノートブック内で基礎実験から実行する。
縮小確認では既存のデータキャッシュを利用しており、提供元サーバーからの再ダウンロード可用性を保証するものではない。
配布用コピーのJupyter実行IDは `20260906T085527875448Z` である。
Jupyterの通信ポートを起動できる環境で確認した。
GUI上の操作ではなく、新規カーネルでノートブック全体を順次実行する検証である。

## 数学と時間順序

単一出力と相関の一致、正規方程式、多出力の逆回帰、時系列分割、時刻の一致、風向変換、ブロック再標本化をテストした。
新しい一括再標本化の逆推定は、標本を明示的に複製して汎用推定器で計算した結果と照合した。
元の単位を変えても、共通座標に戻した係数と再構成の安定性指標が変わらないことを確認した。
全664窓で較正終了時刻が評価開始時刻より前にある。
予測時間別の3項の和と標準化再構成の最大差は `2.23e-15` 未満である。
保存済みのローリング再構成とも一致することを実行時に検査した。

## 今回修正した問題

日時の整数表現には、ナノ秒とマイクロ秒などの違いがある。
単位をそろえずに暦グリッドを検査すると、pandasの版によって正しい時刻を不正として扱う場合があった。
共通パッケージでは日時精度を明示してから判定するよう修正し、0.3.3とした。
逆作用素の推定式と正則化は0.3.2から変更していない。
連続事例の判定も、整数の内部単位に依存しない時間差比較にした。
日時精度を変えた場合と欠測で区間が切れる場合を回帰テストに追加した。

降雨などが一定の窓を見かけ上安定なゼロ係数として解釈しないよう、係数の欠測表示と有効窓数を追加した。
予測差の分位点分類は、絶対的な増加や減少ではないことをラベルと説明に反映した。
TeX表では長い識別子の折り返しを修正し、数値CSVは変更していない。
体裁修正前後の集計CSVと図のPNGも照合した。
46表の組版はエラーと列幅のはみ出し警告なしで完了した。
長い監査表も含む補足資料用の出力であり、全表を本文に載せる想定ではない。

## 保存と公開用ファイル

v9.3のノートブックと元の実行結果は保存している。
ノートブック原本のSHA-256は次のとおりである。

```text
6d6679ae808fee5e304bc72f9d2c4dc8f88cc0293af30486bd5760b425358df5
```

旧0.3.2のソースは `archives/tsaime_0.3.2_source_before_v9_4.zip` に保存した。
開発途中の実行は論文用の最終実行と区別し、公開用ZIPへ入れない。
公開用ZIPは必要なコードと文書を列挙して作り、生データ、非公開トレース、実行結果、監査作業物を除外する。
ノートブックは出力を除いた配布用コピーとし、実行済み原本を消去しない。
ソフトウェアのwheelとtar.gzには汎用tsaimeだけを収録する。
APR固有の取得処理、実験設定、追加説明検証、図表生成はノートブックにある。

## 完成と未確認を区別する

この記録のPASSは計算上の検査に通ったことを示す。
新規性、統計的優位性、物理解釈、採択、データ利用権の保証ではない。
観測系列に時間依存と地点間依存があり、点別区間の数を独立した再現回数と解釈しない。
逆推定の再標本化は予測器の再学習不確実性を含まない。
追加検証はv9.3を見た後の探索的検証である。

NIESの提供ページとデータ内の利用条件表記の不一致は、公開前に確認が必要である。
図に観測値を描いた場合も、この確認から除外しない。
研究用のソフトウェアライセンスはデータの利用条件を上書きしない。
GitHubへの送信、タグ付け、Zenodo登録は行っていない。
公開DOIと確定タグは、本人が実際に公開した後に追記する。
本文に使う図表の選択、説明文、最終誌面での配置は執筆時に確認する。
