# v9.3の固定する実験方針

2026年9月6日。
v9.2の結果を見た後の修正であり、未観察の事前登録実験とは扱わない。
採択や有意差を実行成功の条件にしない。

## 数学と評価対象

標準化した入力Xと予測ベクトルYについて、ts-AIMEは、標本数nで割った残差平方和と、係数行列のFrobeniusノルムの二乗をλ倍した罰則項の和を最小化する。
λ選択も較正用部分だけで求めた入力の標準偏差を使い、変数間で同じ標準化誤差を平均する。
二次逆回帰も同じ入力尺度、同じλ候補を使う。
残差平方和を最小化する実装では罰則係数をnλとする。
単一出力の選択は検証期間内だけで行い、最終評価の最良値から選ばない。

## 比較対象

固定逆再構成と過去28日から次7日への逐次逆再構成で、過去平均、相関総和、相関平均、検証期間で選んだ単一出力、線形ts-AIMEを同じ標本で評価する。
固定逆再構成では二次逆回帰も比較する。
気象変数とPM2.5履歴を分けて表示し、各変数の不利な結果も残す。

## 時間と不確実性

予測器の学習、選択、最終評価の期間と入力10変数はv9.2から変更しない。
すべての性能差は実時刻を残した非循環の移動ブロックbootstrapで評価し、7、14、28日の結果を保存する。
主表示は28日、他の長さは感度分析とする。
これは学習済みモデルを条件とする点ごとの95%区間であり、同時信頼区間や予測区間ではない。
週シフト対照は月曜0時で区切った完全な週に限定し、端の不完全週を明示的に除く。
シフトごとの標本数を保存し、循環シフトの順位を厳密な非定常時系列のp値と呼ばない。

## 合成検証

独立標本での検証では、生成時の母集団共分散に対する作用素回復を主指標とし、標本共分散を条件とする整合性を別列に残す。
連続時系列では定常共分散を持つAR(1)入力に既知の区分的順写像を適用し、既知の出力雑音を加える。
過去窓だけから推定し、次の区間の再構成、母集団作用素からの誤差、変化点からの距離を保存する。
変化をまたぐ窓と単一レジーム内の窓を区別し、追従が遅れる区間も図に示す。
気象過程のシミュレータではなく作用素推定の検証として記述する。

## 順写像との関係

Y=FX+r、C=Cov(X,r)とすると、Sxy=ΣxFᵀ+C、Syy=FΣxFᵀ+FC+CᵀFᵀ+Σrである。
残差の無相関を仮定した近似、Cの相対ノルム、全項を戻した代数的一致を区別する。
全項を戻した一致を独立の実証や因果性の証拠としない。

## 保存と公開

v9.2のノートブック、結果ZIPを保持する。
v9.3は別名のノートブックとタイムスタンプ付き結果ディレクトリを使う。
APR固有の処理はノートブック内に置き、共通の暦時間統計だけをパッケージに追加する。
時刻ごとの予測、入力、標準化、較正境界、誤差はローカルに保存する。
結果ZIPには図、集計表、TeX、実行記録を含め、観測値を含む私的再現用トレースは含めない。
NIESのライセンス表記の不一致は研究コードの修正では解消できないため、公開前の未解決事項として残す。
